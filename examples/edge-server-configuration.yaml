---
# edge server configuration parameters
# cache parameters
cache.expiration: 60
cache.cleanup: 60
cache.size: 10

# server api parameters
api.port: 1323
etcd.address: "localhost:2379"

# container management
containers.memory: 1024
container.expiration: 30
janitor.interval: 30

# offloading parameters
server_url: "http://127.0.0.1:1324/invoke/"
offloading: true